# 表情包素材库：Agent 安装与使用指引

## 项目与入口

本仓库是供人和 AI Agent 使用的本地表情包素材集合。无需构建、启动服务或登录聊天账号。Agent 通过文件工具读取清单、筛选描述，再用图片工具预览与展示原图。

| 文件 | 用途 |
| --- | --- |
| `README.md` | 面向使用者的介绍、安装命令、搜索与校验示例 |
| `AGENTS.md` | Agent 安装、使用和维护流程 |
| `manifest.json` | 唯一的图片清单，必须位于仓库根目录 |
| `emoticons/` | 原图目录，文件名为 `<md5>.<format>` |

当前支持 `gif`、`png`、`jpg`、`webp`。条目数、文件大小与描述以清单为准，不把 README 的快照数字当成查询结果。

## 安装到本地

1. 确认用户指定的素材位置、已有安装和可用磁盘空间。当前原图约 1 GiB，Git 克隆还需要额外空间。使用用户已有路径；未指定时，可在当前工作区的素材目录安装，并在完成后报告绝对路径。
2. 新安装可在选定的父目录执行：

   ```bash
   git clone --depth 1 https://github.com/9Ashwin/wechat-emoticon-pack.git
   cd wechat-emoticon-pack
   ```

3. 如果目标目录已经存在，先检查根目录的 `manifest.json`、`emoticons/` 和 `git remote -v`，确认它确实是本仓库。复用已有安装，不覆盖已有目录或为同一用途重复下载。
4. 读取根目录清单，确认 `schema_version` 为 `1`。按 README 的“更新与校验”示例检查全部文件的路径、大小、MD5 和 SHA-256，并检查一张候选图可以正常打开。
5. 报告实际安装路径、清单条目数、校验结果，以及当前客户端能否显示图片。下载完成、校验通过和对话中显示成功分别陈述，不用其中一项代替另外两项。

没有 Git 时，可下载仓库 ZIP 并解压到所选目录，再做同样的校验。ZIP 副本的更新需重新下载，不执行 `git pull`。文档的命令示例使用 Bash 和 Python 3；其他环境可通过等价的文件与终端工具完成。

## 让其他项目引用

仓库指引需要被实际读取才能生效。用户只想在当前对话用图时，读取此文件即可；用户希望跨项目持续使用时，在其授权的项目或全局指引中追加一段引用，保留原有内容。

引用应包含：素材库真实绝对路径、此文件的绝对路径、按描述检索并预览后展示原图的流程，以及用户偏好的使用频率。不要把整份文件覆盖到已有 `AGENTS.md`，也不要仅记录当前安装机器的临时路径供其他电脑照抄。

## 在聊天中选图

1. 根据用户当前语境选择短关键词。例如确认信息可搜索“收到”，协作可搜索“击拳”，工作间隙可搜索“咖啡”。先应用用户对角色、风格和重复图片的限制。
2. 解析 `manifest.json`，按 `caption` 搜索；优先具体描述，再考虑自动分类标签。输出少量候选的绝对路径、描述、格式和大小，不把整份清单或图片字节塞进上下文。可直接使用 README 的“搜索表情包”示例。
3. 先预览最相关的 1–3 张，确认文字、画面和语气适合当前对话。“自动识别：动图；人物”只表示分类，不证明人物在做什么。不能只根据哈希文件名猜内容。
4. 若无匹配，换一次同义词或较宽泛的标签。仍无合适候选就继续文字交流，不为一张反应图启动大批识别或下载。
5. 一次通常展示一张，避免重复近期使用的 MD5。用户希望经常配图时，可以在工作进展、验证通过和问题解决等自然节点使用，不把选图变成额外等待。

下载并完成首次校验后，日常使用读取本地清单与候选文件即可，不需要每次联网更新或遍历全部图片。

## 展示原图

- 用实际文件路径和当前客户端支持的图片预览、附件或媒体输出机制。客户端支持本地 Markdown 图片时，使用 `![简短描述](/实际绝对路径/emoticons/文件名.gif)`，不要用相对路径或把图片语法包在代码块里。
- 保留 GIF 和其他动画格式的原文件。预览工具只显示单帧时，只描述已经看到的内容；查看动画需要支持该格式的工具。
- 当前客户端不能显示本地图片时，说明限制并提供可用的文件附件或路径，不声称图片已经送达。
- 在当前助手对话展示与向外部聊天发送是不同操作。向其他人或群聊发送，需要用户对目标和发送行为的明确授权，以及实际可用的发送工具。

## 更新与导入

Git 安装在拉取更新前查看 `git status --short`、当前分支和 remote；工作区干净且对应本仓库时，运行 `git pull --ff-only`。有本地收藏或描述改动时先保护这些改动，出现冲突时保留双方内容，不执行强制重置或强制推送来消除冲突。

更新后重新读取清单，并校验文件。Git 命令成功不等于所有图片已验证或已导入另一个应用。

导入其他工具时：

- 选择包含 `manifest.json` 的仓库根目录作为包目录，按目标工具要求放置整个目录。
- 确认工具支持 `schema_version=1`，以及 `filename` 中的 `emoticons/` 相对路径。
- 按目标工具提供的接口导入，不猜测工具名或配置路径；汇报实际成功、跳过和失败数量。
- 按 MD5 去重并用 SHA-256 检查内容冲突。原图相同且描述更新时，按目标工具的描述合并规则处理。

## 维护收藏与描述

用户要求新增收藏或补充描述时，按以下约定修改：

1. **先拿到原图。** 仅有聊天记录、缩略图、远程地址或零字节占位不算获得原图。检查实际格式和可读性，保留动画；不要把网页响应保存成图片。
2. **按内容命名。** 对最终原始字节计算 MD5、SHA-256 和大小，保存为 `emoticons/<md5>.<format>`。不重新压缩图片以凑格式，也不通过修改清单哈希掩盖损坏。
3. **去重并保护原文件。** 已有相同 MD5 时再核对 SHA-256；两者一致则复用文件，内容冲突时停止该条目的写入并报告。新增条目按 MD5 排序，保持现有清单风格。
4. **描述基于所见。** 采用“主体 + 动作或表情 + 可见文字 + 适用语气”的简短中文描述。看不清就记录不确定，不猜角色身份、文字或动画过程，不用粗略自动标签覆盖已有具体描述。
5. **只保存必要内容。** 清单保持现有字段，不添加聊天来源身份、群名、账号信息、访问凭据、下载密钥、数据库或私有路径。公开新增素材应具有相应的分享权限。
6. **成套发布。** 新图片与对应清单条目一并完成后再提交，确保清单不会指向缺失文件。只改描述时仅更新 `caption`，保留内容哈希和文件。

不为检索需求额外添加嵌入数据库、模型服务、安装器或未经请求的自动化。当前需求可用清单与本地图片完成。

## 提交前检查

- 查看仓库、分支、工作区和暂存范围，保留无关修改；按用户指定的范围提交与推送。
- 新增或替换图片后运行 README 的完整校验示例，并检查清单路径与图片一一对应、没有重复 MD5 或未登记的图片。
- 预览新增图片和修改过描述的图片，确认描述符合实际画面。
- 只改文档时检查本地链接、图片预览、清单示例和可执行命令；运行 `git diff --check`。
- 图片数量或格式分布变化时更新 README 的快照数字。始终保持根目录清单与 `emoticons/` 子目录约定。
- 报告真实完成的检查和交付状态；没有执行远端导入、客户端展示或发布时，不声称已经完成。
